<UserControl x:Class="WeatherApp.Views.RecommendationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WeatherApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border Style="{StaticResource GlassPanelStyle}"
            Padding="14">
        <Grid TextElement.Foreground="White">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="#F3D477"
                    CornerRadius="2"
                    Margin="0,1,0,1" />

            <StackPanel Grid.Column="1"
                        Margin="12,0,0,0">
                <TextBlock Text=" Today's Recommendation"
                           FontSize="11"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource SecondaryWeatherTextBrush}"
                           Margin="0,0,0,4" />
                <TextBlock Text="{Binding RecommendationText}"
                           FontSize="14"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </Border>
    
</UserControl>
